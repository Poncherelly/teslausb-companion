// Hardcoded for now. Once the BLE pairing wizard hands off the app's
// admin session, the app will learn the Pi's address during setup
// instead of assuming this fixed IP.
export const PI_SERVICE_URL = 'http://192.168.50.103:3000';
